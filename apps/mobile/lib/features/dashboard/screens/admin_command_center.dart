import 'package:flutter/material.dart';

class AdminCommandCenter extends StatelessWidget {
  const AdminCommandCenter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Admin Command Center'));
  }
}
